const BaseController = require("./BaseController");
const express = require('express');
const app = express();

module.exports = new (class AccessLevelController extends BaseController {
  async GetAllAccessList(req, res, next) {

    
  }
})();
